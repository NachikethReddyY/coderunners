{
  "name": "@coderunners/fixture-codecast-react",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./manifest.json": "./manifest.json"
  },
  "files": [
    "dist",
    "audio",
    "draft.json",
    "expected-states.json",
    "manifest.json",
    "workspace"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "pnpm --dir workspace dev",
    "fixture:audio": "node scripts/generate-audio.mjs",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
