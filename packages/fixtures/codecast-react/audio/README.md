# Golden audio baseline

`codecast.wav` is a deterministic 70-second mono PCM WAV used to prove bundle
shape, duration, and event bounds before the media-worker lane supplies aligned
narration. Regenerate it with `pnpm fixture:audio` from the fixture package.
