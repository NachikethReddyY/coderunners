{
  "name": "coderunners",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@coderunners/contracts": "workspace:*",
    "@coderunners/local-host": "workspace:*",
    "turbo": "catalog:"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "test": "turbo run test --filter=!coderunners-react-habit-toggle",
    "test:conformance": "sh test/conformance/run.sh"
  }
}
